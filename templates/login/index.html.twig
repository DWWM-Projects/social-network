{% extends 'base.html.twig' %}

{% block title %}Connexion - My Social Network{% endblock %}

{% block body %}
    <form action="{{ path('app_login') }}" method="post">
        <div class="w-1/5 text-center mx-auto flex flex-col">
            
                <label for="username">Email:</label>
                <input class="text-center" type="text" id="username" name="_username" value="{{ last_username }}"/>

                <label for="password">Password:</label>
                <input class="text-center" type="password" id="password" name="_password"/>           
            
            <div class="my-6">
                <label>
                <input type="checkbox" name="_remember_me"/>
                    Rester connecté
                </label>
            </div>

            {# If you want to control the URL the user is redirected to on success
            <input type="hidden" name="_target_path" value="/account"/> #}

            <button class="w-2/5 mx-auto rounded-lg bg-blue-300 hover:bg-blue-600 duration-500 text-center text-white p-2" type="submit">login</button>
        </div>
    </form>
{% endblock %}
